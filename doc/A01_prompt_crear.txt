consulta 
estoy trabajando con conemu en windows
tengo una estructura de directorios en comun para un conjunto de proyectos 
para cada proyecto, armo un conjunto de terminales conemu
creo un "task" en conemu, con la lineas que definen cada terminal que necesito
creo un shortcut que apunta a conemu, y ejecuta la task de ese proyecto 


supongamos que quiero agregar el proyecto newpro

esta es la estructura de directorios que uso:

directorios / destino 
c:\per                  root de todo 
c:\per\pro              root de todos los proyectos 
c:\per\pro\newpro       root del proyecto de que estoy agregando

dentro del directorio del proyecto, suelo tener 
src          fuentes 
build        todos los objetos para crear ejecutables, normalmente, los ejecutables los dejo aca 
env          aqui van todos los scripts que crean variables de ambientes, si es que las necesito 
doc          documentacion 

despues 
para utilizar este "ambiente" , creo una "task" en conemu 
y ahi configuo las terminales que quiero usar, 
normalmente 
root        terminal que se para en el root del proyecto 
git         terminal para trabajar con git , generalmente, tambien es el root del proyecto
src         terminal que hace cd al directorio src
build       terminal , idem build
doc         terminal , idem doc

entonces, 
1) creo una task normalmente "Erno_newpro"    (uso el nombre del proyecto : newpro )
2) creo un shorcut, 
target:    "C:\Program Files\ConEmu\ConEmu64.exe" -run {Erno_newpro}
start in:   "C:\Program Files\ConEmu\"

3) en la task 
en el campo "tak parameters" pong esto:    /dir "c:\per\pro\newpro"      (el root lo pongo con el nombre del proyecto)

4) configuro las terminales :
en este caso, al abrir cada terminal, ejecuta  c:\per\pro\newpro\env\env_project.bat
se para en un directorio que cuelga  en c:\per\pro\newpro   
y le pone un nombre : 
en este ejemplo, terminales 
pipeline, aux, root, git, src,build1,build2 



cmd.exe /k "call "%ConEmuWorkDir%\env\env_project.bat" & cd /d "%ConEmuWorkDir%"" -new_console:t:"Root"

cmd.exe /k "call "%ConEmuWorkDir%\env\env_project.bat" & cd /d "%ConEmuWorkDir%"" -new_console:t:"Git"

cmd.exe /k "call "%ConEmuWorkDir%\env\env_project.bat" & cd /d "%ConEmuWorkDir%\src"" -new_console:t:"src"

cmd.exe /k "call "%ConEmuWorkDir%\env\env_project.bat" & cd /d "%ConEmuWorkDir%\build"" -new_console:t:"Build1"

cmd.exe /k "call "%ConEmuWorkDir%\env\env_project.bat" & cd /d "%ConEmuWorkDir%\build"" -new_console:t:"Build2"

cmd.exe /k "call "%ConEmuWorkDir%\env\env_project.bat" & cd /d "%ConEmuWorkDir%\doc"" -new_console:t:"Doc"



mi consulta es :
puedo armar un script, o programa en c++. o .NET , lo que mas te parezca 
de manera que yo indique , ya sea por pantalla on por un archivo config_project.txt 
que me arme todo lo relacionado a conemu ?

por ejemplo, 
en el archivo de configuracion, yo prondria. _



nombre de proyecto :   newpro
root :                              c:\per\pro\newpro

task:                                Erno_newpro
parametro dir                 /dir "c:\per\pro\newpro" 
shortcut_name               NewPro

terminales  (para root, git, src,build1,build2,doc  )
numero, directorio raiz, directorio, nombre de la pantalla, nombre del bat de config a ejecutar 
1, root, %ConEmuWorkDir%\,  . , root , env_project.bat 
2, git,  %ConEmuWorkDir%\,  . , git  , env_projec.at 
2, src,  %ConEmuWorkDir%\,  src , src  , env_projec.at 

y con esta info , obtener alguna tool que me genere 
a) el shortcut 
b) la task
c) las lineas de configuracion en la task de todas las terminales 



me haces un roadmap de como podemos construir una herramienta (script, programa etc )
que me genere ese shortcut ?


